services:
  - type: web
    name: market-oracle-server
    runtime: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: CLIENT_ORIGIN
        sync: false
